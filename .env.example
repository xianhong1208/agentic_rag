# Copy to .env and fill in real values. .env overrides any global shell env for this project.
# Postgres (pgvector) connection. No `+asyncpg` — this project uses sync psycopg2.
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5444/agentic_rag
